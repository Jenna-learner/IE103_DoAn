{
  "name": "fnb-backend",
  "version": "1.0.0",
  "description": "FnB Chain Management System - Backend API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:init": "psql -h $DB_HOST -U $DB_USER -d $DB_NAME -f sql/01_schema_ddl.sql && psql -h $DB_HOST -U $DB_USER -d $DB_NAME -f sql/02_triggers_procedures.sql && psql -h $DB_HOST -U $DB_USER -d $DB_NAME -f sql/03_security_roles.sql && psql -h $DB_HOST -U $DB_USER -d $DB_NAME -f sql/04_sample_data.sql",
    "db:schema": "psql -h $DB_HOST -U $DB_USER -d $DB_NAME -f sql/01_schema_ddl.sql",
    "db:seed": "psql -h $DB_HOST -U $DB_USER -d $DB_NAME -f sql/04_sample_data.sql"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
